#pragma once
#include <string>

struct Client
{
	Client(string p_name, int p_age, string p_login, string p_pass, int p_id = 0, string status = "user");
public:
	string nam
	string login
	string pass;
	string role;
	int age,id;
};